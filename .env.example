# Configuration Django
DEBUG=True
SECRET_KEY=votre-cle-secrete-a-changer-en-production

# Base de données
# Pour SQLite (développement local)
DATABASE_URL=sqlite:///db.sqlite3

# Pour PostgreSQL (production)
# DATABASE_URL=postgres://user:password@localhost:5432/uganc_db

# Hôtes autorisés (séparés par des virgules)
ALLOWED_HOSTS=localhost,127.0.0.1

# Langue et fuseau horaire
LANGUAGE_CODE=fr-fr
TIME_ZONE=Africa/Conakry

# Email (optionnel - pour réinitialisation mot de passe)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=votre-email@example.com
EMAIL_HOST_PASSWORD=votre-mot-de-passe
